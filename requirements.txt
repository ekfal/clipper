# Every runtime dependency. Install with:  pip install -r requirements.txt
# ffmpeg itself is a system package, not a Python one:  apt install ffmpeg
# Fonts too:  apt install fonts-dejavu-core fonts-noto-color-emoji
#
# To render clips and nothing else, requirements-clip.txt is a quarter of this
# list. It is pulled in below rather than duplicated, so nothing drifts.
-r requirements-clip.txt

# --- footage ---------------------------------------------------------------
# yt-dlp comes from requirements-clip.txt. gdown is only for Drive links, so
# it stays here: a link-in-link-out job never reaches it.
gdown>=5.2.0

# --- Clippo session + public profile stats ---------------------------------
# after install:  playwright install chromium
playwright>=1.49.0

# --- YouTube upload --------------------------------------------------------
google-api-python-client>=2.150.0
google-auth>=2.36.0
google-auth-oauthlib>=1.2.0

# --- accounts dashboard ----------------------------------------------------
fastapi>=0.115.0
uvicorn>=0.32.0
# FastAPI needs this for the dashboard's Form() endpoints; without it every
# POST there fails at import time rather than at request time.
python-multipart>=0.0.18
